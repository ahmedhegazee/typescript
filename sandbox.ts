const text = "Ahmed";
console.log(text);
const inputs = document.querySelectorAll('input');
console.log(inputs);

inputs.forEach((input) => {
    console.log(input);
});

// tsc sandbox.js -w 
//to compile and watch js files